---
phase: 02-test-harness
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/integration/golden/add.spec.ts
  - tests/integration/golden/migrate.spec.ts
  - tests/integration/golden/create-qwik.spec.ts
autonomous: true
requirements:
  - TEST-02
  - TEST-03
  - TEST-04

must_haves:
  truths:
    - "ADD-01 through ADD-03 tests each assert correct exit code for add command scenarios"
    - "MIG-01 through MIG-05 tests assert migration behavior including ordering constraint and idempotency"
    - "CRE-01 and CRE-02 tests invoke create-qwik binary and assert on scaffolded output"
    - "Every test includes an assert on result.status (exit code)"
    - "MIG-02 test asserts exit 0 on user cancel (cancellation is not error)"
    - "CRE tests clean up generated temp directories in teardown hooks"
    - "MIG-01 has positive assertion that files DO contain @qwik.dev/core after migration (fails against stub)"
    - "MIG-04 has positive assertion that files DO contain @qwik.dev/router (fails against stub, not just vacuously checking absence of @qwik.dev/core-city)"
    - "MIG-02, MIG-03, MIG-05 pass vacuously against stubs (documented with comments); they become meaningful in Phase 5"
  artifacts:
    - path: "tests/integration/golden/add.spec.ts"
      provides: "ADD-01 through ADD-03 golden-path tests"
      contains: "assert.strictEqual(status"
    - path: "tests/integration/golden/migrate.spec.ts"
      provides: "MIG-01 through MIG-05 golden-path tests"
      contains: "assert.strictEqual(status"
    - path: "tests/integration/golden/create-qwik.spec.ts"
      provides: "CRE-01 and CRE-02 golden-path tests"
      contains: "assert.strictEqual(status"
  key_links:
    - from: "tests/integration/golden/migrate.spec.ts"
      to: "tests/fixtures/fx-02/"
      via: "getFixturePath('fx-02') temp copy"
      pattern: "getFixturePath.*fx-02"
    - from: "tests/integration/golden/migrate.spec.ts"
      to: "tests/fixtures/fx-03/"
      via: "getFixturePath('fx-03') temp copy"
      pattern: "getFixturePath.*fx-03"
    - from: "tests/integration/golden/create-qwik.spec.ts"
      to: "tests/integration/helpers/cli.ts"
      via: "import { runCreateQwik }"
      pattern: "import.*runCreateQwik.*helpers/cli"
---

<objective>
Write 10 golden-path Japa integration tests covering add command (ADD-01 through ADD-03), migrate-v2 command (MIG-01 through MIG-05), and create-qwik scaffolding (CRE-01, CRE-02).

Purpose: These remaining 10 of 25 spec-first tests define the behavioral contract for Phases 5-6. Tests are intentionally red — they assert on FINAL expected behavior. The add and migrate tests cover the highest-risk CLI behaviors (destructive file mutation, integration installation).
Output: 3 spec files under `tests/integration/golden/` with 10 tests total.
</objective>

<execution_context>
@/Users/jackshelton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jackshelton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-test-harness/02-RESEARCH.md
@specs/PARITY-TEST-PLAN.md
@.planning/phases/02-test-harness/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — test helpers -->
From tests/integration/helpers/cli.ts:
```typescript
export interface CliResult {
  status: number;
  stdout: string;
  stderr: string;
}
export function runCli(args: string[], cwd?: string): CliResult;
export function runCreateQwik(args: string[], cwd?: string): CliResult;
```

From tests/integration/helpers/fixtures.ts:
```typescript
export const FIXTURES_DIR: string;
export function getFixturePath(...segments: string[]): string;
export function setMtimePast(filePath: string, secondsAgo?: number): void;
export function setMtimeFuture(filePath: string, secondsAhead?: number): void;
```

<!-- Fixture directories from Plan 01 -->
Fixtures used by this plan:
- tests/fixtures/fx-01/ — Minimal Qwik v2 app (for ADD tests)
- tests/fixtures/fx-02/ — v1 project with @builder.io/qwik imports (for MIG tests)
- tests/fixtures/fx-03/ — v1 project with ts-morph in devDeps (for MIG-03)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write add and create-qwik golden-path tests</name>
  <files>tests/integration/golden/add.spec.ts, tests/integration/golden/create-qwik.spec.ts</files>
  <action>
Create `tests/integration/golden/add.spec.ts` with:
- Import `{ test } from "@japa/runner"`, `{ runCli } from "../helpers/cli.js"`, `{ getFixturePath } from "../helpers/fixtures.js"`
- Import `{ cpSync, rmSync, mkdirSync } from "node:fs"`, `{ join } from "node:path"`, `os from "node:os"`
- FX_01 via `getFixturePath("fx-01")`
- Use `group.each.setup` / `group.each.teardown` for temp dir copies (add command writes files to project).
- **ADD-01** test group "ADD-01 -- qwik add with --skipConfirmation":
  - Copy FX-01 to tmpDir. `runCli(["add", "cloudflare-pages", "--skipConfirmation=true"], tmpDir)`, assert `status === 0`. Will FAIL until Phase 5 implements add command (stub currently prints "add command (stub)" and exits 0 — but test should also assert integration files were written, which stub does not do).
  - Additional assertion: check that some integration-related file exists in tmpDir (e.g., an adapter file). This specific assertion will fail — correct red state.
- **ADD-02** test group "ADD-02 -- qwik add with --projectDir":
  - Copy FX-01 to tmpDir. Create `sub/` subdirectory. Copy FX-01 package.json into sub/. `runCli(["add", "--projectDir=./sub", "--skipConfirmation=true"], tmpDir)`, assert `status === 0`. Will FAIL until Phase 5.
- **ADD-03** test group "ADD-03 -- qwik add nonexistent integration":
  - Copy FX-01 to tmpDir. `runCli(["add", "nonexistent-integration", "--skipConfirmation=true"], tmpDir)`, assert `status === 1` (unknown integration should error). Will FAIL until Phase 5 (stub exits 0).

Create `tests/integration/golden/create-qwik.spec.ts` with:
- Import `{ test } from "@japa/runner"`, `{ runCreateQwik } from "../helpers/cli.js"`
- Import `{ existsSync, rmSync, readFileSync } from "node:fs"`, `{ join } from "node:path"`, `os from "node:os"`
- **CRE-01** test group "CRE-01 -- create-qwik empty":
  - Use group.each.setup to create a unique tmpDir, group.each.teardown to rmSync it.
  - `const outDir = join(tmpDir, "my-app")`
  - `runCreateQwik(["empty", outDir])`, assert `status === 0`.
  - Assert `existsSync(join(outDir, "package.json"))` — scaffolded project has package.json.
  - Assert package.json contains `@qwik.dev/core` in dependencies.
  - Assert `existsSync(join(outDir, "src/routes"))` — routes dir created.
  - Will FAIL: `bin/create-qwik.ts` does not exist yet (Phase 6). The test will error with module-not-found. This is the correct red state per Research open question 1.
- **CRE-02** test group "CRE-02 -- create-qwik library":
  - Same tmpDir setup/teardown pattern.
  - `const outDir = join(tmpDir, "my-lib")`
  - `runCreateQwik(["library", outDir])`, assert `status === 0`.
  - Assert `existsSync(join(outDir, "package.json"))`.
  - Assert the library starter structure has no base layer merge artifacts (verify package.json does not contain a `starterApp` key or similar — exact assertion depends on Phase 6 implementation; for now assert package.json exists and is valid JSON).
  - Will FAIL: same module-not-found reason as CRE-01.

CRITICAL: Every test MUST assert `result.status` (exit code). This satisfies TEST-03.

NOTE on CRE tests: These tests call `runCreateQwik` which points to `bin/create-qwik.ts` — a file that does not exist until Phase 6. The spawnSync call will return status !== 0 and the test will fail. This is intentional and correct for the spec-first approach. CRE-01 produces what PARITY-TEST-PLAN.md calls FX-07 (scaffolded empty starter) and CRE-02 produces FX-08 (scaffolded library starter) — these are runtime test outputs, not static fixtures.
  </action>
  <verify>
    <automated>node --import tsx/esm bin/test.ts --files "tests/integration/golden/add.spec.ts" --files "tests/integration/golden/create-qwik.spec.ts" 2>&1 | tail -5</automated>
  </verify>
  <done>add.spec.ts has ADD-01 through ADD-03 tests with exit code assertions; create-qwik.spec.ts has CRE-01 and CRE-02 tests with exit code assertions; all tests use temp dir isolation; test runner executes without crashing</done>
</task>

<task type="auto">
  <name>Task 2: Write migrate-v2 golden-path tests</name>
  <files>tests/integration/golden/migrate.spec.ts</files>
  <action>
Create `tests/integration/golden/migrate.spec.ts` with:
- Import `{ test } from "@japa/runner"`, `{ runCli } from "../helpers/cli.js"`, `{ getFixturePath } from "../helpers/fixtures.js"`
- Import `{ cpSync, rmSync, readFileSync, existsSync, readdirSync } from "node:fs"`, `{ join } from "node:path"`, `os from "node:os"`
- FX_02 and FX_03 via `getFixturePath`
- Use `group.each.setup` / `group.each.teardown` for temp dir copies — migration is DESTRUCTIVE (rewrites source files). NEVER run migrate on the original fixture.

- **MIG-01** test group "MIG-01 -- full migration golden path":
  - Copy FX-02 to tmpDir. The test needs to simulate user confirmation (yes). For Phase 2, invoke `runCli(["migrate-v2"], tmpDir)`. Since the stub just prints and exits 0, the test asserts on FINAL behavior:
  - Assert `status === 0`.
  - Read `tmpDir/src/app.tsx` — assert it does NOT contain `@builder.io/qwik` (imports rewritten).
  - Read `tmpDir/src/routes/index.tsx` — assert it does NOT contain `@builder.io/qwik-city` (renamed to `@qwik.dev/router`).
  - Read `tmpDir/package.json` — assert `ts-morph` is NOT in dependencies or devDependencies.
  - **POSITIVE assertion (ensures red against stub):** Assert `tmpDir/src/app.tsx` DOES contain `@qwik.dev/core` (the new import after migration). This WILL FAIL against the stub because the stub does not rewrite files — the file still has `@builder.io/qwik`, not `@qwik.dev/core`.
  - Will FAIL: stub does not rewrite files.

- **MIG-02** test group "MIG-02 -- user cancels migration":
  - Copy FX-02 to tmpDir. The cancel scenario requires stdin "no" to the confirmation prompt. For Phase 2, this is aspirational — the test documents expected behavior. Use `runCli(["migrate-v2"], tmpDir)` (stub ignores prompts).
  - Assert `status === 0` (cancellation is NOT an error — exit 0 is MUST PRESERVE per COMPATIBILITY-CONTRACT).
  - Read `tmpDir/src/app.tsx` — assert it STILL contains `@builder.io/qwik` (no files modified on cancel).
  - The "still contains" assertion will PASS against the stub (which does not modify files). That is acceptable — the test correctly passes for the wrong reason in Phase 2 and will need the cancel signal in Phase 5 to be a real test. Add a comment: `// NOTE: passes vacuously against stub — stub does not modify files. TODO Phase 5: pipe "n\n" to stdin to test real cancel path`.

- **MIG-03** test group "MIG-03 -- ts-morph idempotency guard":
  - Copy FX-03 to tmpDir. `runCli(["migrate-v2"], tmpDir)`.
  - Assert `status === 0`.
  - Read `tmpDir/package.json` — assert `ts-morph` is STILL in devDependencies (not removed by migration).
  - Add comment: `// NOTE: passes vacuously against stub — stub does not remove ts-morph. Becomes meaningful in Phase 5 when migration actually runs.`

- **MIG-04** test group "MIG-04 -- substring ordering constraint":
  - Copy FX-02 to tmpDir. `runCli(["migrate-v2"], tmpDir)`.
  - Assert `status === 0`.
  - Read ALL files under `tmpDir/src/` — assert NONE contain `@qwik.dev/core-city`. This string would appear if `@builder.io/qwik` replacement runs BEFORE `@builder.io/qwik-city` replacement (the substring ordering bug). The correct order is: replace `@builder.io/qwik-city` first, then `@builder.io/qwik` last.
  - **POSITIVE assertion (ensures red against stub):** Assert `tmpDir/src/routes/index.tsx` DOES contain `@qwik.dev/router` (the replacement for `@builder.io/qwik-city`). This WILL FAIL against the stub because the stub does not rewrite files — the file still has `@builder.io/qwik-city`.

- **MIG-05** test group "MIG-05 -- gitignore-respected traversal":
  - Copy FX-02 to tmpDir. Create a `tmpDir/dist/bundle.js` file containing `@builder.io/qwik` text. `runCli(["migrate-v2"], tmpDir)`.
  - Assert `status === 0`.
  - Read `tmpDir/dist/bundle.js` — assert it STILL contains `@builder.io/qwik` (file under dist/ was not modified because dist/ is in .gitignore).
  - Add comment: `// NOTE: passes vacuously against stub — stub does not modify any files. Becomes meaningful in Phase 5 when migration rewrites src/ files but respects .gitignore for dist/.`

CRITICAL: Every test MUST assert `result.status` (exit code). This satisfies TEST-03.

NOTE on vacuous passes: MIG-02, MIG-03, and MIG-05 will pass against stubs because their assertions check that files are NOT modified, and stubs don't modify files. This is documented with comments. MIG-01 and MIG-04 have additional POSITIVE assertions (files MUST contain new imports) that ensure they genuinely fail against stubs. When Phase 5 implements real migration, all 5 tests become meaningful.
  </action>
  <verify>
    <automated>node --import tsx/esm bin/test.ts --files "tests/integration/golden/migrate.spec.ts" 2>&1 | tail -5</automated>
  </verify>
  <done>migrate.spec.ts has MIG-01 through MIG-05 tests; each test asserts exit code; tests use temp dir copies of FX-02/FX-03 (never mutate originals); MIG-01 and MIG-04 have positive assertions that FAIL against stubs (assert new imports exist); MIG-02/03/05 have vacuous-pass comments; test runner executes without crashing</done>
</task>

</tasks>

<verification>
- `node --import tsx/esm bin/test.ts` runs to completion without crashing the test runner itself
- All 7 golden-path spec files exist under tests/integration/golden/ (4 from Plan 02 + 3 from this plan)
- 25 tests total across all 7 files
- Every test contains an `assert.strictEqual(status, ...)` call (TEST-03)
- MIG-01 and MIG-04 genuinely fail against stubs (positive assertions on new import names)
- MIG-02, MIG-03, MIG-05 documented as vacuously passing against stubs
- Existing 39 unit tests remain green
- mtime helpers (TEST-04) are exercised in check-client.spec.ts from Plan 02
</verification>

<success_criteria>
- add.spec.ts contains ADD-01 through ADD-03 tests
- migrate.spec.ts contains MIG-01 through MIG-05 tests
- create-qwik.spec.ts contains CRE-01 and CRE-02 tests
- Every single test asserts on result.status (exit code) — zero exceptions
- All migrate tests use temp dir copies (never modify original fixtures)
- MIG-01 asserts src/app.tsx contains @qwik.dev/core (fails against stub)
- MIG-04 asserts src/routes/index.tsx contains @qwik.dev/router (fails against stub)
- Test runner executes all files without crashing (tests may fail as expected red state)
- Full test suite (39 unit + 25 integration) runs via `node --import tsx/esm bin/test.ts`
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-harness/02-03-SUMMARY.md`
</output>
