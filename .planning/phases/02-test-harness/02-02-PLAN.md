---
phase: 02-test-harness
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/integration/golden/simple.spec.ts
  - tests/integration/golden/build.spec.ts
  - tests/integration/golden/new.spec.ts
  - tests/integration/golden/check-client.spec.ts
autonomous: true
requirements:
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "VER-01 test asserts exit 0 and bare semver regex on stdout"
    - "JOKE-01 test asserts exit 0 and presence of joke text in stdout"
    - "BUILD-01 through BUILD-04 tests each assert correct exit code and build script execution"
    - "NEW-01 through NEW-05 tests each assert file creation or duplicate guard error"
    - "CHK-01 through CHK-03 tests assert correct check-client behavior for missing/stale/fresh states"
    - "Every test includes an assert on result.status (exit code)"
  artifacts:
    - path: "tests/integration/golden/simple.spec.ts"
      provides: "VER-01 and JOKE-01 golden-path tests"
      contains: "assert.strictEqual(status"
    - path: "tests/integration/golden/build.spec.ts"
      provides: "BUILD-01 through BUILD-04 golden-path tests"
      contains: "assert.strictEqual(status"
    - path: "tests/integration/golden/new.spec.ts"
      provides: "NEW-01 through NEW-05 golden-path tests"
      contains: "assert.strictEqual(status"
    - path: "tests/integration/golden/check-client.spec.ts"
      provides: "CHK-01 through CHK-03 golden-path tests"
      contains: "assert.strictEqual(status"
  key_links:
    - from: "tests/integration/golden/simple.spec.ts"
      to: "tests/integration/helpers/cli.ts"
      via: "import { runCli }"
      pattern: "import.*runCli.*helpers/cli"
    - from: "tests/integration/golden/check-client.spec.ts"
      to: "tests/integration/helpers/fixtures.ts"
      via: "import { setMtimePast, setMtimeFuture }"
      pattern: "import.*setMtime.*helpers/fixtures"
---

<objective>
Write 15 golden-path Japa integration tests covering simple commands (VER-01, JOKE-01), build command (BUILD-01 through BUILD-04), new command (NEW-01 through NEW-05), and check-client command (CHK-01 through CHK-03).

Purpose: These 15 of 25 total spec-first tests define the behavioral contract for Phases 3-4 and 6 (check-client). Tests are intentionally red — they assert on FINAL expected behavior, not stub behavior.
Output: 4 spec files under `tests/integration/golden/` with 15 tests total.
</objective>

<execution_context>
@/Users/jackshelton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jackshelton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-test-harness/02-RESEARCH.md
@specs/PARITY-TEST-PLAN.md
@.planning/phases/02-test-harness/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — test helpers -->
From tests/integration/helpers/cli.ts:
```typescript
export interface CliResult {
  status: number;
  stdout: string;
  stderr: string;
}
export function runCli(args: string[], cwd?: string): CliResult;
export function runCreateQwik(args: string[], cwd?: string): CliResult;
```

From tests/integration/helpers/fixtures.ts:
```typescript
export const FIXTURES_DIR: string;
export function getFixturePath(...segments: string[]): string;
export function setMtimePast(filePath: string, secondsAgo?: number): void;
export function setMtimeFuture(filePath: string, secondsAhead?: number): void;
```

<!-- Fixture directories from Plan 01 -->
Fixtures available:
- tests/fixtures/fx-01/ — Minimal Qwik v2 app with echo-based build scripts
- tests/fixtures/fx-04/ — Empty project (no routes/components)
- tests/fixtures/fx-05/ — Project with existing route + component (duplicate guard)
- tests/fixtures/fx-06/ — Built project with dist/q-manifest.json
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write simple and build command golden-path tests</name>
  <files>tests/integration/golden/simple.spec.ts, tests/integration/golden/build.spec.ts</files>
  <action>
Create `tests/integration/golden/simple.spec.ts` with:
- Import `{ test } from "@japa/runner"` and `{ runCli } from "../helpers/cli.js"`
- **VER-01** test group "VER-01 -- qwik version":
  - Test "outputs bare semver string, no label prefix, exit 0": `runCli(["version"])`, assert `status === 0`, assert `stdout.trim()` matches `/^\d+\.\d+\.\d+$/m`. This will FAIL until Phase 3 implements the version command with real semver output (stub currently prints "version command (stub)").
- **JOKE-01** test group "JOKE-01 -- qwik joke":
  - Test "outputs joke text, exit 0, no file writes": `runCli(["joke"])`, assert `status === 0`, assert `stdout.length > 0` (basic content check). A more specific assertion: stdout should contain at least two lines (setup + punchline). Will FAIL until Phase 3 implements the joke command with real jokes array.

Create `tests/integration/golden/build.spec.ts` with:
- Import `{ test } from "@japa/runner"`, `{ runCli } from "../helpers/cli.js"`, `{ getFixturePath } from "../helpers/fixtures.js"`
- Resolve FX-01 path: `const FX_01 = getFixturePath("fx-01")`
- **BUILD-01** test group "BUILD-01 -- qwik build":
  - Test "runs build.client first, then parallel scripts, exit 0": `runCli(["build"], FX_01)`, assert `status === 0`, assert stdout contains "build-client-ok" (proves build.client script ran). Will FAIL until Phase 4 implements build orchestration.
- **BUILD-02** test group "BUILD-02 -- qwik build preview":
  - Test "triggers build.preview instead of build.server, exit 0": `runCli(["build", "preview"], FX_01)`, assert `status === 0`, assert stdout contains "build-preview-ok". Will FAIL until Phase 4.
- **BUILD-03** test group "BUILD-03 -- qwik build --mode staging":
  - Test "forwards --mode to build scripts, exit 0": `runCli(["build", "--mode", "staging"], FX_01)`, assert `status === 0`. Will FAIL until Phase 4.
- **BUILD-04** test group "BUILD-04 -- qwik build with failing script":
  - Test "sets exitCode 1 but does not abort parallel scripts": This test needs a mutated FX-01 with a failing script. Create a setup that copies fx-01 to a temp dir and overwrites `build.server` script to `"exit 1"`. Use `os.tmpdir()`, `cpSync`, and cleanup in group teardown. `runCli(["build"], tmpDir)`, assert `status === 1`. Will FAIL until Phase 4.

CRITICAL: Every test MUST assert `status` (exit code). This satisfies TEST-03. Use `assert.strictEqual(status, 0)` or `assert.strictEqual(status, 1)` — never omit exit code assertion.
  </action>
  <verify>
    <automated>node --import tsx/esm bin/test.ts --files "tests/integration/golden/simple.spec.ts" --files "tests/integration/golden/build.spec.ts" 2>&1 | tail -5</automated>
  </verify>
  <done>simple.spec.ts has VER-01 and JOKE-01 tests; build.spec.ts has BUILD-01 through BUILD-04 tests; every test asserts exit code; test runner executes without crashing (tests may fail — that is the expected red state)</done>
</task>

<task type="auto">
  <name>Task 2: Write new command and check-client golden-path tests</name>
  <files>tests/integration/golden/new.spec.ts, tests/integration/golden/check-client.spec.ts</files>
  <action>
Create `tests/integration/golden/new.spec.ts` with:
- Import `{ test } from "@japa/runner"`, `{ runCli } from "../helpers/cli.js"`, `{ getFixturePath } from "../helpers/fixtures.js"`
- Import `{ existsSync, cpSync, rmSync, mkdirSync } from "node:fs"`, `{ join } from "node:path"`, `os from "node:os"`
- FX_04 and FX_05 paths via `getFixturePath`
- Each test that writes files should work in a temp copy of the fixture (to avoid cross-test pollution). Use `group.each.setup` to copy fixture to tmpdir and `group.each.teardown` to clean up.
- **NEW-01** test group "NEW-01 -- qwik new /dashboard":
  - Copy FX-04 to tmpDir. `runCli(["new", "/dashboard"], tmpDir)`, assert `status === 0`, assert `existsSync(join(tmpDir, "src/routes/dashboard/index.tsx"))`. Will FAIL until Phase 4.
- **NEW-02** test group "NEW-02 -- qwik new counter":
  - Copy FX-04 to tmpDir. `runCli(["new", "counter"], tmpDir)`, assert `status === 0`, assert `existsSync(join(tmpDir, "src/components/counter.tsx"))`. Will FAIL until Phase 4. Note: the component name casing depends on implementation — the test checks for the file's existence.
- **NEW-03** test group "NEW-03 -- qwik new /blog/post.md":
  - Copy FX-04 to tmpDir. `runCli(["new", "/blog/post.md"], tmpDir)`, assert `status === 0`, assert `existsSync(join(tmpDir, "src/routes/blog/post.md"))`. Will FAIL until Phase 4.
- **NEW-04** test group "NEW-04 -- duplicate guard":
  - Copy FX-05 to tmpDir. `runCli(["new", "/dashboard"], tmpDir)`, assert `status === 1`, assert stderr or stdout contains "already exists". Will FAIL until Phase 4 (stub exits 0).
- **NEW-05** test group "NEW-05 -- nested directory creation":
  - Copy FX-04 to tmpDir. `runCli(["new", "/nested/deep/route"], tmpDir)`, assert `status === 0`, assert `existsSync(join(tmpDir, "src/routes/nested/deep/route/index.tsx"))`. Will FAIL until Phase 4.

Create `tests/integration/golden/check-client.spec.ts` with:
- Import `{ test } from "@japa/runner"`, `{ runCli } from "../helpers/cli.js"`, `{ getFixturePath, setMtimePast, setMtimeFuture } from "../helpers/fixtures.js"`
- Import `{ cpSync, rmSync } from "node:fs"`, `{ join } from "node:path"`, `os from "node:os"`
- FX-01 and FX-06 paths via `getFixturePath`
- Use `group.each.setup` / `group.each.teardown` for temp dir copies (mtime manipulation mutates state).
- **CHK-01** test group "CHK-01 -- no dist directory":
  - Copy FX-01 to tmpDir (FX-01 has no dist/). `runCli(["check-client", "src", "dist"], tmpDir)`, assert `status === 0`, assert stdout contains "build-client" (proves build.client was triggered). Will FAIL until Phase 6 implements check-client logic.
- **CHK-02** test group "CHK-02 -- up-to-date manifest":
  - Copy FX-06 to tmpDir. `setMtimeFuture(join(tmpDir, "dist", "q-manifest.json"))` to make manifest newer than src. `runCli(["check-client", "src", "dist"], tmpDir)`, assert `status === 0`, assert stdout is empty or only whitespace (silent success). Will FAIL until Phase 6.
- **CHK-03** test group "CHK-03 -- stale src":
  - Copy FX-06 to tmpDir. `setMtimePast(join(tmpDir, "dist", "q-manifest.json"))` to make manifest older than src. `runCli(["check-client", "src", "dist"], tmpDir)`, assert `status === 0`, assert stdout contains "build-client" (proves rebuild triggered). Will FAIL until Phase 6.

CRITICAL: Every test MUST assert `result.status` (exit code). This satisfies TEST-03.

NOTE on test isolation: new.spec.ts and check-client.spec.ts both mutate filesystem state. Always work on temp copies, never on the original fixture directories.
  </action>
  <verify>
    <automated>node --import tsx/esm bin/test.ts --files "tests/integration/golden/new.spec.ts" --files "tests/integration/golden/check-client.spec.ts" 2>&1 | tail -5</automated>
  </verify>
  <done>new.spec.ts has NEW-01 through NEW-05 tests; check-client.spec.ts has CHK-01 through CHK-03 tests; every test asserts exit code; all tests use temp dir copies for isolation; test runner executes without crashing</done>
</task>

</tasks>

<verification>
- `node --import tsx/esm bin/test.ts` runs to completion without crashing the test runner
- All 4 spec files exist under tests/integration/golden/
- 15 tests total across the 4 files (VER-01, JOKE-01, BUILD-01-04, NEW-01-05, CHK-01-03)
- Every test contains an `assert.strictEqual(status, ...)` call
- Existing 39 unit tests remain green
</verification>

<success_criteria>
- simple.spec.ts contains VER-01 and JOKE-01 tests
- build.spec.ts contains BUILD-01 through BUILD-04 tests
- new.spec.ts contains NEW-01 through NEW-05 tests
- check-client.spec.ts contains CHK-01 through CHK-03 tests
- Every single test asserts on result.status (exit code) — zero exceptions
- Test runner executes all files without crashing (tests may fail as expected red state)
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-harness/02-02-SUMMARY.md`
</output>
