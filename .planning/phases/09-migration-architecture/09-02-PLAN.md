---
phase: 09-migration-architecture
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/commands/migrate/index.ts
  - src/router.ts
autonomous: true
requirements: [MIGR-02, MIGR-05, VTST-02]

must_haves:
  truths:
    - "qwik upgrade calls the new orchestrator (not runV2Migration directly)"
    - "qwik migrate-v2 alias still works and routes to the same MigrateProgram"
    - "Running upgrade on an already-v2 project produces no file changes and exits 0"
    - "Existing Japa golden-path tests that were passing before Phase 9 still pass"
    - "Both Vitest unit tests and Japa integration tests pass"
  artifacts:
    - path: "src/commands/migrate/index.ts"
      provides: "MigrateProgram calling runUpgrade orchestrator"
      contains: "runUpgrade"
  key_links:
    - from: "src/commands/migrate/index.ts"
      to: "src/upgrade/orchestrator.ts"
      via: "import runUpgrade"
      pattern: "import.*runUpgrade.*from.*upgrade/orchestrator"
    - from: "src/router.ts"
      to: "src/commands/migrate/index.ts"
      via: "dynamic import for upgrade and migrate-v2"
      pattern: "upgrade.*import.*commands/migrate"
---

<objective>
Wire the MigrateProgram to call the new `runUpgrade` orchestrator instead of `runV2Migration` directly, and verify the full integration (both Japa golden-path tests and Vitest unit tests pass).

Purpose: Complete the migration architecture by connecting the command entry point to the new orchestration layer, ensuring the upgrade command now has version detection, chaining, and no-op guard capabilities.
Output: Updated MigrateProgram, all tests green.
</objective>

<execution_context>
@/Users/jackshelton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jackshelton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-migration-architecture/09-RESEARCH.md
@.planning/phases/09-migration-architecture/09-01-SUMMARY.md

@src/commands/migrate/index.ts
@src/router.ts

<interfaces>
<!-- From Plan 01 (must exist before this plan runs) -->

From src/upgrade/orchestrator.ts:
```typescript
export const MIGRATION_REGISTRY: MigrationStep[];
export async function runUpgrade(rootDir: string): Promise<void>;
```

From src/upgrade/detect-version.ts:
```typescript
export function detectInstalledVersion(rootDir: string): string | null;
```

From src/upgrade/chain-builder.ts:
```typescript
export interface MigrationStep {
  version: string;
  label: string;
  run: (rootDir: string) => Promise<void>;
}
export function buildMigrationChain(
  fromVersion: string | null,
  toVersion: string,
  registry: MigrationStep[],
): MigrationStep[];
```

<!-- Existing MigrateProgram that needs updating -->

From src/commands/migrate/index.ts (CURRENT — will be modified):
```typescript
import { runV2Migration } from "../../migrate/run-migration.js";
// ...
protected async execute(_input: MigrateInput): Promise<number> {
  // ...
  await runV2Migration(rootDir);
  return 0;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire MigrateProgram to runUpgrade orchestrator</name>
  <files>src/commands/migrate/index.ts</files>
  <action>
    Update `src/commands/migrate/index.ts`:

    1. **Change the import:** Replace `import { runV2Migration } from "../../migrate/run-migration.js"` with `import { runUpgrade } from "../../upgrade/orchestrator.js"`.

    2. **Update execute() body:** Replace `await runV2Migration(rootDir)` with `await runUpgrade(rootDir)`.

    Everything else in MigrateProgram stays the same — the confirmation prompt, the `bye()` on cancel, the `process.cwd()` usage, the `return 0` at the end.

    **Do NOT change:**
    - The `registerCommand("migrate-v2", ...)` call — the command name stays `migrate-v2`
    - The router's `upgrade` alias — it already points to `./commands/migrate/index.js` and does not need changing
    - The confirmation prompt flow — the orchestrator runs AFTER user confirms
    - The `scanBoolean` call — it must still precede the upgrade call

    **Verify `src/router.ts` is unchanged:** The router already has both `"migrate-v2"` and `"upgrade"` entries pointing to `./commands/migrate/index.js`. No changes needed.

    Run `npx tsc --noEmit` to verify the import path resolves correctly.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "runUpgrade" src/commands/migrate/index.ts && grep -qv "runV2Migration" src/commands/migrate/index.ts</automated>
  </verify>
  <done>
    - MigrateProgram imports `runUpgrade` from `../../upgrade/orchestrator.js`
    - No reference to `runV2Migration` remains in `src/commands/migrate/index.ts`
    - `tsc --noEmit` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify full test suite (Vitest unit + Japa integration)</name>
  <files></files>
  <action>
    Run both test suites to verify the full integration:

    1. **Vitest unit tests:** `pnpm vitest run tests/unit/upgrade/ --reporter=verbose`
       - All 19 tests from Plan 01 must pass
       - Confirm version detection, chain building, and orchestration logic works

    2. **Japa integration tests:** `node bin/test.ts`
       - MIG-01 through MIG-05 tests must still pass (or be in the same state as before Phase 9)
       - The migrate-v2 command now goes through the orchestrator, but the actual migration behavior is identical — `runUpgrade` calls `runV2Migration` under the hood when the chain builder determines v2 migration is needed

    3. **Build verification:** `pnpm run build` (or `npx tsdown`)
       - The build must succeed — tsdown needs to follow the import chain from `src/router.ts` -> `src/commands/migrate/index.ts` -> `src/upgrade/orchestrator.ts` -> `migrations/v2/index.ts`
       - If tsdown fails because `migrations/v2/` is outside the `src/` tree, the fix is to verify that `tsdown.config.ts` does not restrict resolution to `src/` only (current config has `entry: ['src/index.ts', 'src/router.ts', 'bin/qwik.ts']` with no explicit `resolve` restrictions)

    If any tests fail:
    - If Japa MIG-* tests fail due to import path issues: the `runV2Migration` internal imports use relative `./` paths which should still work from `migrations/v2/`. Debug the specific error.
    - If tsdown build fails: check if `migrations/` directory needs to be included in tsconfig `include` array or if there's a rootDir constraint. The current tsconfig has `rootDir: "."` (set in Phase 1 to allow `bin/` TypeScript files), so `migrations/` should be within scope.
    - If Vitest tests fail: debug the specific assertion. The mocking strategy may need adjustment if module resolution differs.

    Fix any issues found. Do not leave failing tests.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/upgrade/ --reporter=verbose && pnpm run build && node bin/test.ts</automated>
  </verify>
  <done>
    - All Vitest unit tests pass (19 tests across 3 files)
    - tsdown build succeeds with no errors
    - Japa integration tests pass (MIG-01 through MIG-05 in same state as before Phase 9, all other golden-path tests unaffected)
    - The upgrade command flows through: MigrateProgram.execute() -> runUpgrade() -> detectInstalledVersion() -> buildMigrationChain() -> runV2Migration() (when needed)
  </done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/unit/upgrade/` — all unit tests pass
- `pnpm run build` — build succeeds
- `node bin/test.ts` — Japa integration tests pass
- `grep "runUpgrade" src/commands/migrate/index.ts` — orchestrator is wired
- `grep -r "runV2Migration" src/commands/` produces no output — direct call removed
</verification>

<success_criteria>
- MigrateProgram calls `runUpgrade` (not `runV2Migration` directly)
- Both `migrate-v2` and `upgrade` router entries work identically
- All Vitest unit tests pass (version detection, chain building, orchestration, no-op guard)
- All Japa golden-path tests pass
- tsdown build succeeds
- `tsc --noEmit` zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-migration-architecture/09-02-SUMMARY.md`
</output>
