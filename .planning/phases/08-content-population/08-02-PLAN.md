---
phase: 08-content-population
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/joke/jokes.ts
  - adapters/ (deleted)
autonomous: true
requirements: [STRT-04, TOOL-04]

must_haves:
  truths:
    - "qwik joke draws from the full upstream joke pool (50+ entries)"
    - "The top-level adapters/ directory no longer exists in the repository"
    - "Existing JOKE-01 test still passes"
  artifacts:
    - path: "src/commands/joke/jokes.ts"
      provides: "Full upstream joke pool"
      min_lines: 80
  key_links:
    - from: "src/commands/joke/jokes.ts"
      to: "src/commands/joke/index.ts"
      via: "getRandomJoke() import"
      pattern: "getRandomJoke"
---

<objective>
Replace the hardcoded 10-joke array with the full upstream joke pool from the Qwik repo, and remove the stray top-level adapters/ directory.

Purpose: TOOL-04 requires jokes sourced from the real Qwik repo pool. STRT-04 requires the incorrect top-level adapters/ artifact removed.
Output: Expanded jokes.ts with full upstream content, top-level adapters/ deleted
</objective>

<execution_context>
@/Users/jackshelton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jackshelton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-content-population/08-RESEARCH.md
@src/commands/joke/jokes.ts

<interfaces>
<!-- jokes.ts exports Joke type, JOKES array, and getRandomJoke() -->
<!-- joke/index.ts imports getRandomJoke() and calls it in execute() -->
<!-- SIMP-04: jokes array must be static data within the CLI package (no cross-package import) -->

From src/commands/joke/jokes.ts:
```typescript
export type Joke = [setup: string, punchline: string];
export const JOKES: Joke[] = [ /* currently 10 entries */ ];
export function getRandomJoke(): Joke;
```

From src/commands/joke/index.ts (consumer):
```typescript
import { getRandomJoke } from "./jokes.js";
// Used in JokeProgram.execute() — prints setup + punchline via console.log
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace jokes array with full upstream pool and delete top-level adapters/</name>
  <files>src/commands/joke/jokes.ts, adapters/ (deleted)</files>
  <action>
1. Fetch the upstream jokes data from the Qwik monorepo `build/v2` branch. The jokes live at:
   `https://raw.githubusercontent.com/QwikDev/qwik/build/v2/packages/qwik/src/cli/jokes.json`
   (If this exact path fails, check `starters/` or `packages/qwik/src/cli/` for `jokes.json` on the `build/v2` branch.)

2. Replace the 10-entry `JOKES` array in `src/commands/joke/jokes.ts` with ALL entries from the upstream `jokes.json`. Use Option B from the research: inline the array directly in the TypeScript file. Do NOT use JSON import assertion syntax.

3. Preserve the exact file structure:
   - `export type Joke = [setup: string, punchline: string];` (unchanged)
   - `export const JOKES: Joke[] = [...]` (expanded with all upstream entries)
   - `export function getRandomJoke(): Joke { ... }` (unchanged — same logic, same fallback)

4. Each joke entry is a tuple `[setup, punchline]`. The upstream `jokes.json` format is an array of `[string, string]` tuples. Map directly.

5. Delete the entire top-level `adapters/` directory (contains only `adapters/cloudflare-pages/vite.config.ts`). This is the stray artifact — the correct adapter lives in `stubs/adapters/cloudflare-pages/`. Use `rm -rf adapters/` and `git rm -r adapters/`.

6. Verify:
   - `JOKES.length` is greater than 30 (upstream has 50+)
   - `getRandomJoke()` still works (returns a 2-element tuple)
   - `ls adapters/` returns "No such file or directory"
   - JOKE-01 test passes: `node --import tsx/esm bin/test.ts`
  </action>
  <verify>
    <automated>bash -c 'node --import tsx/esm -e "import { JOKES } from \"./src/commands/joke/jokes.js\"; console.log(\"Jokes:\", JOKES.length); if (JOKES.length < 30) { process.exit(1); }" && ! ls adapters/ 2>/dev/null && echo "PASS" || (echo "FAIL" && exit 1)'</automated>
  </verify>
  <done>jokes.ts contains 50+ joke entries from the upstream Qwik repo pool. getRandomJoke() returns jokes from the expanded pool. The top-level adapters/ directory no longer exists. JOKE-01 test passes.</done>
</task>

</tasks>

<verification>
1. `JOKES.length` is >= 30 (upstream has ~52-60)
2. `getRandomJoke()` returns a valid `[string, string]` tuple
3. `ls adapters/` fails with "No such file or directory"
4. `node --import tsx/esm bin/test.ts` — all existing tests pass (especially JOKE-01)
5. `tsc --noEmit` passes (no type regressions)
</verification>

<success_criteria>
- jokes.ts contains the full upstream joke pool (50+ entries), preserving Joke type and getRandomJoke() signature
- Top-level adapters/ directory is deleted from the repository
- JOKE-01 parity test passes
- tsc --noEmit has zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-content-population/08-02-SUMMARY.md`
</output>
