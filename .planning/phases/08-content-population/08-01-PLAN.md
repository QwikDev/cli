---
phase: 08-content-population
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - stubs/adapters/aws-lambda/
  - stubs/adapters/azure-swa/
  - stubs/adapters/bun/
  - stubs/adapters/cloud-run/
  - stubs/adapters/cloudflare-workers/
  - stubs/adapters/deno/
  - stubs/adapters/express/
  - stubs/adapters/fastify/
  - stubs/adapters/firebase/
  - stubs/adapters/netlify-edge/
  - stubs/adapters/node-server/
  - stubs/adapters/ssg/
  - stubs/adapters/vercel-edge/
  - stubs/features/ (22 directories)
  - stubs/apps/base/
  - stubs/apps/empty/
  - stubs/apps/playground/
  - stubs/apps/library/
autonomous: true
requirements: [STRT-01, STRT-02, STRT-03, STRT-05]

must_haves:
  truths:
    - "stubs/adapters/ contains all 14 adapter directories with valid __qwik__ metadata"
    - "stubs/features/ contains all 22 feature directories with valid __qwik__ metadata"
    - "stubs/apps/ contains all 4 app starter directories (base, empty, playground, library)"
    - "loadIntegrations() discovers all 14 adapters and 22 features"
    - "npm pack --dry-run includes all stubs content in the tarball"
  artifacts:
    - path: "stubs/adapters/aws-lambda/package.json"
      provides: "AWS Lambda adapter integration"
      contains: "__qwik__"
    - path: "stubs/features/tailwind/package.json"
      provides: "Tailwind feature integration"
      contains: "__qwik__"
    - path: "stubs/apps/empty/package.json"
      provides: "Empty app starter"
      contains: "__qwik__"
    - path: "stubs/apps/base/package.json"
      provides: "Base app starter (no __qwik__ key)"
  key_links:
    - from: "stubs/adapters/*/package.json"
      to: "loadIntegrations()"
      via: "__qwik__ key presence"
      pattern: "__qwik__"
    - from: "stubs/features/*/package.json"
      to: "loadIntegrations()"
      via: "__qwik__ key presence"
      pattern: "__qwik__"
---

<objective>
Populate all stubs/ content from the Qwik monorepo build/v2 branch: 13 missing adapters, 22 features, and 4 app starters.

Purpose: Provides the complete integration catalog so `qwik add` shows the full 14-adapter and 22-feature menus, and `stubs/apps/` is ready for create-qwik in Phase 11.
Output: Fully populated stubs/adapters/ (14), stubs/features/ (22), stubs/apps/ (4)
</objective>

<execution_context>
@/Users/jackshelton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jackshelton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-content-population/08-RESEARCH.md
@specs/ASSET-INVENTORY.md
@src/integrations/load-integrations.ts

<interfaces>
<!-- loadIntegrations() discovery: scans stubs/adapters/ and stubs/features/ -->
<!-- Each subdirectory MUST have package.json with __qwik__ key to be discovered -->
<!-- stubs/apps/ is NOT scanned by loadIntegrations() — used by create-qwik only -->

From src/integrations/load-integrations.ts:
```typescript
export async function loadIntegrations(): Promise<IntegrationData[]>;
// Scans stubs/adapters/ and stubs/features/
// Skips directories without package.json or without __qwik__ key
// Sorts by priority descending, then id ascending
```

Required __qwik__ metadata shape in each integration package.json:
```json
{
  "__qwik__": {
    "displayName": "Adapter: Cloudflare Pages",
    "priority": 40
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sparse-checkout upstream and copy all adapters and features into stubs/</name>
  <files>stubs/adapters/ (13 new directories), stubs/features/ (22 new directories)</files>
  <action>
1. Clone the Qwik monorepo with sparse checkout from the `build/v2` branch into a temporary directory:
   ```bash
   TMPDIR=$(mktemp -d)
   git clone --sparse --filter=blob:none --branch build/v2 --depth 1 \
     https://github.com/QwikDev/qwik.git "$TMPDIR/qwik"
   cd "$TMPDIR/qwik"
   git sparse-checkout set starters/adapters starters/features starters/apps
   ```

2. Copy all 13 missing adapter directories from `$TMPDIR/qwik/starters/adapters/` into `stubs/adapters/`. The existing `cloudflare-pages` is already present — overwrite it with the upstream version to ensure consistency. The 14 adapters are:
   `aws-lambda`, `azure-swa`, `bun`, `cloud-run`, `cloudflare-pages`, `cloudflare-workers`, `deno`, `express`, `fastify`, `firebase`, `netlify-edge`, `node-server`, `ssg`, `vercel-edge`

3. Copy all 22 feature directories from `$TMPDIR/qwik/starters/features/` into `stubs/features/`. The 22 features are:
   `auth`, `bootstrap`, `builder.io`, `compiled-i18n`, `csr`, `cypress`, `drizzle`, `leaflet-map`, `orama`, `pandacss`, `partytown`, `playwright`, `postcss`, `prisma`, `react`, `service-worker`, `storybook`, `styled-vanilla-extract`, `tailwind`, `tailwind-v3`, `turso`, `vitest`

   IMPORTANT: `csr` exists on `build/v2` but NOT on `main`. This is correct — source from `build/v2`.

4. Copy the entire directory tree for each integration, not just package.json. Each adapter/feature may include: `adapters/<name>/vite.config.ts`, `src/entry.<name>.tsx`, `public/`, `gitignore` (no dot), `README.md`, etc.

5. Preserve `gitignore` (no-dot) filename convention from upstream — do NOT rename to `.gitignore` in stubs.

6. Remove `.gitkeep` files from `stubs/adapters/` and `stubs/features/` (they were placeholders for empty directories and are now unnecessary).

7. Clean up the temporary clone: `rm -rf "$TMPDIR/qwik"`

8. Verify each adapter's package.json contains `__qwik__` key. Verify count: 14 adapters, 22 features.
  </action>
  <verify>
    <automated>bash -c 'ADAPTERS=$(ls -d stubs/adapters/*/  | wc -l | tr -d " "); FEATURES=$(ls -d stubs/features/*/ | wc -l | tr -d " "); echo "Adapters: $ADAPTERS, Features: $FEATURES"; [[ "$ADAPTERS" -eq 14 ]] && [[ "$FEATURES" -eq 22 ]] && echo "PASS" || (echo "FAIL" && exit 1)'</automated>
  </verify>
  <done>stubs/adapters/ contains exactly 14 directories, each with a package.json containing __qwik__ metadata. stubs/features/ contains exactly 22 directories, each with a package.json containing __qwik__ metadata. No .gitkeep files remain.</done>
</task>

<task type="auto">
  <name>Task 2: Copy app starters into stubs/apps/ and verify tarball inclusion</name>
  <files>stubs/apps/base/, stubs/apps/empty/, stubs/apps/playground/, stubs/apps/library/</files>
  <action>
1. Using the same sparse-checkout approach from Task 1 (or reuse the clone if still available), copy all 4 app starter directories from `$TMPDIR/qwik/starters/apps/` into `stubs/apps/`:
   - `base` — foundation layer; NO `__qwik__` key (intentionally invisible to loadIntegrations)
   - `empty` — priority 1; default interactive selection
   - `playground` — priority 2; sorted first in menu
   - `library` — priority -1; self-contained bootstrap

2. Apply the upstream `isValidFsItem()` filter: do NOT copy `e2e/`, `qwikrouter-test/`, `.rollup.cache/`, `build/`, `server/`, `node_modules/`, lockfiles, `starter.tsconfig.json`, `tsconfig.tsbuildinfo`, or any directory ending in `-test` or containing `.prod`.

3. Preserve `gitignore` (no-dot) filename convention from upstream.

4. Remove `.gitkeep` file from `stubs/apps/` if present.

5. Verify:
   - `stubs/apps/base/package.json` exists and does NOT contain `__qwik__`
   - `stubs/apps/empty/package.json` contains `__qwik__` with priority 1
   - `stubs/apps/playground/package.json` contains `__qwik__` with priority 2
   - `stubs/apps/library/package.json` contains `__qwik__` with priority -1

6. Run `npm pack --dry-run 2>&1` and verify the output includes entries under `stubs/adapters/`, `stubs/features/`, and `stubs/apps/`.
  </action>
  <verify>
    <automated>bash -c 'APPS=$(ls -d stubs/apps/*/ | wc -l | tr -d " "); echo "Apps: $APPS"; [[ "$APPS" -eq 4 ]] && npm pack --dry-run 2>&1 | grep -q "stubs/apps/empty/package.json" && echo "PASS" || (echo "FAIL" && exit 1)'</automated>
  </verify>
  <done>stubs/apps/ contains exactly 4 starter directories (base, empty, playground, library). base has no __qwik__ key. empty/playground/library each have __qwik__ with correct priority values. npm pack --dry-run shows stubs/ content in the tarball output.</done>
</task>

</tasks>

<verification>
1. `ls -d stubs/adapters/*/ | wc -l` outputs 14
2. `ls -d stubs/features/*/ | wc -l` outputs 22
3. `ls -d stubs/apps/*/ | wc -l` outputs 4
4. Every adapter and feature package.json contains `__qwik__` key
5. `stubs/apps/base/package.json` does NOT contain `__qwik__`
6. `npm pack --dry-run` includes all stubs content
7. No `.gitkeep` files remain in stubs/adapters/, stubs/features/, stubs/apps/
8. Existing tests still pass: `node --import tsx/esm bin/test.ts`
</verification>

<success_criteria>
- 14 adapter directories in stubs/adapters/ with valid __qwik__ metadata
- 22 feature directories in stubs/features/ with valid __qwik__ metadata
- 4 app starter directories in stubs/apps/ with correct metadata (base has none)
- npm pack --dry-run lists all stubs content in the tarball
- All existing Japa tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/08-content-population/08-01-SUMMARY.md`
</output>
